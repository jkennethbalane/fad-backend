This repository includes the backend of Fine Arts Department website backend.

Requisites
Ubuntu 20.04 OS

sudo apt-get update
sudo apt-get install docker docker-compose
git clone <this_repository>
docker-compose build
docker compose up -d
